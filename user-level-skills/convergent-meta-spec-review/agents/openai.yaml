interface:
  display_name: "Meta-Spec Reviewer"
  short_description: "Review meta-spec prompts for actionable ambiguity"
  default_prompt: "Use $convergent-meta-spec-review to review this meta-spec and report only concrete blockers and minimal fixes."
